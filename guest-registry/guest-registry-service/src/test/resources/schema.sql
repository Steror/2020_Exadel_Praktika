CREATE TABLE location (
      id INTEGER NOT NULL,
      name VARCHAR(256),
      country VARCHAR(256),
      city VARCHAR(256),
      address VARCHAR(256),
      location_type VARCHAR(256),
      phone_number VARCHAR(256),
      PRIMARY KEY (id)
);
